riscv:
  compileFlags: -compile-top RV32BasicTest
  runFlags: -run-top RV32BasicTest

uvmprimer:01-conventional-test:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:02-interfaces:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:03-classes:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:04-polymorphism:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:05-static-methods:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:06-type-parameters:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:07-factory-pattern:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:08-oop-test:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:09-uvm-test:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=random_test
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=add_test
uvmprimer:10-uvm-components:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=random_test
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=add_test
uvmprimer:11-uvm-environments:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=random_test
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=add_test
uvmprimer:12-analysis-ports:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags: -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so
uvmprimer:13-analysis-ports-test:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=random_test
    - -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=add_test
uvmprimer:14-communication:
  compileFlags: -compile-top top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags: -run-top top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so
uvmprimer:15-communication-test:
  compileFlags: -compile-top Top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=RandomTest
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=AddTest
uvmprimer:16-uvm-reporting:
  compileFlags: -compile-top Top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=RandomTest
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=AddTest
uvmprimer:17-deep-operations:
  compileFlags: -compile-top top
  runFlags: -run-top top
uvmprimer:18-uvm-transactions:
  compileFlags: -compile-top Top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=RandomTest
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=AddTest
uvmprimer:19-uvm-agents:
  compileFlags: -compile-top Top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags: -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=DualTest
uvmprimer:20-uvm-sequences:
  compileFlags: -compile-top Top -incdir $UVM_HOME/src $UVM_HOME/src/uvm_pkg.sv
  runFlags:
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=FibonacciTest
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=FullTest
    - -run-top Top -sv_lib $UVM_HOME/src/dpi/libuvm_dpi.so +UVM_TESTNAME=ParallelTest

vkprimer:01-conventional-test:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:02-interfaces:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:03-classes:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:04-polymorphism:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:05-static-methods:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:06-type-parameters:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:07-factory-pattern:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:08-oop-test:
  compileFlags: -compile-top Top
  runFlags: -run-top Top
vkprimer:17-deep-operations:
  compileFlags: -compile-top Top
  runFlags: -run-top Top

svverif:01-arbiter:
  compileFlags: -compile-top Top
  runFlags: -run-top Top

misc:01-count:
  compileFlags: -compile-top Count
  runFlags: -run-top Count
misc:02-adder:
  compileFlags: -compile-top AdderTop
  runFlags: -run-top AdderTop
misc:03-multiplier:
  compileFlags: -compile-top MultiplyFunctionTest -compile-top MultiplyModuleTest
  runFlags:
    - -run-top MultiplyFunctionTest
    - -run-top MultiplyModuleTest
misc:04-comb:
  compileFlags: >
    -compile-top BitScanReverseTest
    -compile-top IsPowerOfTwoTest
    -compile-top LogPowerOfTwoTest
    -compile-top EqualTest
    -compile-top VectorEqualTest
    -compile-top SevenSegmentDecoderTest
    -compile-top PopulationCountTest
    -compile-top IsGeqTest
  runFlags:
    - -run-top BitScanReverseTest
    - -run-top IsPowerOfTwoTest
    - -run-top LogPowerOfTwoTest
    - -run-top EqualTest
    - -run-top VectorEqualTest
    - -run-top SevenSegmentDecoderTest
    - -run-top PopulationCountTest
    - -run-top IsGeqTest
misc:05-alu:
  compileFlags: >
    -compile-top Sr32Test
    -compile-top Sll32Test
    -compile-top Sft32Test
    -compile-top CmpTest
    -compile-top Ltu32Test
    -compile-top Lt32Test
    -compile-top FaTest
    -compile-top RcaTest
    -compile-top AddSubTest
    -compile-top AluTest
  runFlags:
    - -run-top Sr32Test
    - -run-top Sll32Test
    - -run-top Sft32Test
    - -run-top CmpTest
    - -run-top Ltu32Test
    - -run-top Lt32Test
    - -run-top FaTest
    - -run-top RcaTest
    - -run-top AddSubTest
    - -run-top AluTest
misc:06-cache:
  compileFlags: -compile-top CacheTop
  runFlags: -run-top CacheTop
